## Takeaways

- Use semantic elements (`header`, `main`, `section`, `article`) instead of `div` everywhere.
- Flexbox for one-dimensional layouts, Grid for two-dimensional ones.
- Design for phones first, then scale up with `@media (min-width: ...)`.
